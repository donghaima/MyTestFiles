#!/usr/bin/python
# file: chunk-goid-packing.py

def packing(stream_rate, chunk_interval, total_time):
    total_bytes_in_chunk = stream_rate * chunk_interval / 8;
    total_bytes = stream_rate * total_time / 8;

    while 

    
stream_rate = 3750000  # 3.75Mbps 
chunk_interval = 3600  # 1 hour
